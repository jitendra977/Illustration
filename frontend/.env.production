# ==============================
# Production Environment
# ==============================

# Frontend domain
VITE_HOST=nishanaweb.cloud

# Backend API location (Nginx routes to /api)
# Nginx → /api  → backend:8000
VITE_API_BASE_URL=https://nishanaweb.cloud/api

# Enable Docker/Nginx HMR (optional but safe to keep)
VITE_DOCKER_ENV=true

# Frontend UI environment
VITE_ENVIRONMENT=production